import { Card, CardProps } from 'antd';
import React from 'react';

export type Less3CardProps = CardProps & {};
const Less3Card = (props: Less3CardProps) => {
  return <Card {...props} />;
};

export default Less3Card;
