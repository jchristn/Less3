import { Space, SpaceProps } from 'antd';
import React from 'react';

export type Less3SpaceProps = SpaceProps & {};
const Less3Space = (props: Less3SpaceProps) => {
  return <Space {...props} />;
};

export default Less3Space;
