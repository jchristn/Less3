import { Avatar, AvatarProps } from 'antd';
import React from 'react';

export type Less3AvatarProps = AvatarProps;

const Less3Avatar = (props: Less3AvatarProps) => {
  return <Avatar {...props} />;
};

export default Less3Avatar;
