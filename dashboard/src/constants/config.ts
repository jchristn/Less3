export const apiEndpointURL = 'http://localhost:8000/';

export const MIN_PASSWORD_LENGTH = 8;

export const keepUnusedDataFor = 300; //5mins

// API Key for authentication
export const API_KEY = 'less3admin';

// Mock flag - set to false when API is ready
export const MOCK_VALIDATE_CONNECTIVITY = true;
export const MOCK_BUCKETS = true;
