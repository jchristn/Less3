You are tasked with building v3.0.0 of Less3.  Create a new branch feature/v3.0.0 and enter it.  Update the csproj and docker assets with the updated versions accordingly.  

Version 3.0.0 will have the following new features:
- Multi-tenancy - follow the requirements in c:\code\agents\requirements.  Tenants will be top level, buckets, objects, tags, ACLs, users, and credentials will be subordinate to tenants, and should have a tenant ID property in the objects and in the database tables.  The table name should be tenants.
- Indexes should be applied on the tenant ID columns in each of the tables, and compound indexes should be included where appropriate given the query patterns the code has.
- Less3-specific REST API - add a set of REST endpoints (following the Watson7 method described in c:\code\agents\requirements) including OpenAPI and Swagger metadata to provide the user with create, read (enumeration, retrieve), update, delete, and exists APIs, under a standard REST URL schema, for instance, /api/v1, structured as /api/v1/{type}/{id} for standard CRUD, and for logic operations (for instance, multi-delete) where POST is used, use /api/v1/{type}/{operation}.  Enumeration APIs MUST follow the EnumerationQuery and EnumerationResult pattern described in the referenced docs.  Include creation of S3_API.md (S3 API documentation) and REST_API.md (Less3 REST API documentation)
- Testing using Touchstone as described in c:\code\agents\requirements, with Test.Shared as the source of truth for tests, Test.Automated (Touchstone CLI runner), Test.Xunit (Touchstone Xunit adapter), and Test.Nunit (Touchstone Nunit adapter).  This should stand up a temporary Less3 server, pre-configure it, test it with both AWS S3 APIs as well as Less3-REST APIs

Additionally:
- Database IDs should be converted from integers to string IDs generated by PrettyID (Nuget package) using K-sortable IDs, with prefixes, and a total length of 32 characters.  You can see the PrettyID source in c:\code\misc\prettyid, but use the NuGet package.  The ID column should remain with the name id.
- The credential table should be renamed credentials for consistency.

The dashboard needs several updates:
- Add management of tenants.  Refer to the requirements in c:\code\agents\requirements
- The navbar/TOC should be organized into sections as follows:
  HOME
  MANAGE
    Buckets
    Objects
  CONFIGURE
    Tenants
    Users
    Credentials
  OPERATE
    Request History
    API Explorer
- The Home page should have additional KPI cards at the top, I'll let you suggest which ones should be included
- The Logout link in the upper right should be only the icon, no text

Further dashboard improvements:
  1. Dashboard Health Overview
     Add a compact “node status” band to Home: server version, uptime, database type/reachable, storage path writable, free disk, temp upload count, request-history retention, last cleanup run. Back it with a small /admin/health endpoint. This keeps monitoring simple without introducing
     a metrics stack.

  2. Server-Side Querying For Dashboard Lists
     Add pagination/filter/sort params to admin list endpoints, especially request history. Today request history is effectively SELECT * in src/Less3/Database/Sqlite/Queries/RequestHistoryQueries.cs:31, then filtered client-side. Start with limit, offset or cursor, startUtc, endUtc,
     method, status, success, sourceIp, requestType, userId, accessKey.  Use the EnumerationQuery and EnumerationResult pattern discussed above with the administrative APIs

  3. Request History 2.0
     Turn Request History into the main troubleshooting surface: saved filters, failed-only toggle, status family filters, slow request filter, copy as cURL, CSV export, and grouped views by request type, bucket, user, access key, source IP. The current page is useful but mostly tabular.

  4. Dashboard Reporting
     Add lightweight report cards: requests/min, failure rate, p50/p95 latency, top buckets by bytes, top buckets by request count, top failed request types, top access keys. Use request history and the existing dashboard stats model rather than a new analytics subsystem.

  5. Bucket Detail Pages
     Instead of keeping most bucket actions in row menus, add a bucket detail page with tabs: Overview, Objects, Activity, Tags, ACL, Versioning, Settings. The Buckets table already combines bucket rows with stats in dashboard/src/page/buckets/BucketsPage.tsx:573; a detail page would
     make that information operator-friendly.

  6. Object Explorer Pagination And Versions
     The object UI warns when results are truncated in dashboard/src/page/objects/ObjectsPage.tsx:1408, but it does not expose continuation navigation. Add prefix-aware pagination, version browsing, delete-marker visibility, and “restore/copy version” workflows. This aligns with existing
     versioning support rather than adding new storage semantics.

  7. Safer Credential Management
     Keep secrets out of normal metadata views. Generate access/secret keys server-side, show the secret once on create, support rotate/disable/delete, and show last-used/last-failed from request history. Right now the dashboard persists the admin key in localStorage in dashboard/src/
     services/sdk.service.ts:121, which is acceptable for local/dev but should get a “session-only” option.

  8. Management Console
     Add a simple admin Settings/Maintenance page: request-history retention, purge history, cleanup temp uploads, verify object DB rows vs blob files, export config summary, and show migration status. This fits Less3’s single-node nature better than background orchestration.  Further, the user should be able to modify the Less3 JSON configuration file and update the server's running copy of those settings where appropriate.  The settings page should show the settings in a FORM that is WELL-DESIGNED, and not just a big JSON BLOB that is editable.  The page should clearly articulate which fields, when changed, demand a reboot before they take effect

  10. API Explorer Polish
     The API Explorer already has curated S3/admin operations in dashboard/src/page/api-explorer/ApiExplorerPage.tsx:50. Add bucket/user/credential dropdown injection, saved request collections, environment export/import, and generated examples from current server config.  Add the Less3 REST API as well.

  11. Small Admin API Contract Cleanup
     Normalize endpoint naming over time, especially requesthistory vs dashboard route request-history. Ensure the backend exposes an OpenAPI document for both S3 APIs and Less3-REST APIs. This helps dashboard development without changing the platform model.

  12. Dashboard E2E And Release Smoke Tests
     Add Playwright smoke coverage for login, buckets, objects, request history, API Explorer, and credential flows. The backend S3 tests are relatively strong; dashboard regressions are the bigger risk now.

Do not worry about backwards compatibility.  Assume net-new deployments.  However, a document called MIGRATING_V2_TO_V3.md should be created outlining the manual database changes a user should undertake to migrate to v3.0.0.

Please write a plan called V3_PLAN.md and ensure 1) it is fully compliant with c:\code\agents\requirements, 2) explicitly calls out a post-development task to evaluate the codebase against those requirements, 3) and is actionable, that is, a developer can annotate progress and completion.
